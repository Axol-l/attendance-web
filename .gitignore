# ══════════════════════════════════════════════════════════════
# 说明：本仓库**刻意不包含**任何真实业务数据、内部文档与运维脚本。
#       任何与具体公司/人员/账期相关的材料都不入库。
# ══════════════════════════════════════════════════════════════

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ── 虚拟环境 ──
venv/
env/
ENV/
.venv/

# ── Django ──
*.log
logs/*
!logs/.gitkeep
db.sqlite3
staticfiles/

# ── 环境变量（含口令）──
# 只忽略具体的 .env*，不忽略模板文件：.env.example 是给人照抄的样例。
.env
.env.local
.env.production
.env.*.local

# ── 备份文件 ──
*.sql
*.sql.zip
backup_files/

# ── 测试与覆盖率 ──
.pytest_cache/
.coverage
htmlcov/
tox.ini

# ── IDE / 操作系统 ──
.vscode/
.idea/
.cursor/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# ══════════════════════════════════════════════════════════════
# 以下为**按隐私要求刻意排除**的内容
# ══════════════════════════════════════════════════════════════

# ── 考勤原始数据与生成的报表：含员工姓名、工号等个人信息 ──
*.xlsx
*.xls
media/
uploads/
reports/
参考数据/

# ── 报表模板：模板来自内部流程，其文档属性里带有创建者信息，
#    且属于具体企业的表格格式，不随仓库分发。
#    部署时请自备模板，见 README「报表模板」一节。 ──
attendance/templates_xlsx/

# ── 内部文档：业务口径确认单、权限矩阵、迁移手册等，含内部信息 ──
#    例外：docs/modules/ 是纯技术模块文档，脱敏后随仓库发布。
#    ⚠️ 不能直接写 `docs/` 再 `!docs/modules/` —— Git 的规则是
#       「父目录被排除后，其下的文件无法再被包含」。必须改成 `/docs/*`
#       这种只匹配一层的形式，才能把子目录单独捞回来。
/docs/*
!/docs/modules/

# ── 校验与运维脚本：按真实姓名/账期做对账，且含真实样本路径 ──
tools/

# ── 其它可能含业务数据的产物 ──
*.csv
*.json
!package.json
*.zip
